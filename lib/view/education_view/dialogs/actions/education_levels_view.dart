import 'package:flutter/material.dart';

class EducationLevelsView extends StatelessWidget {
  const EducationLevelsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
    );
  }
}
