import 'package:flutter/material.dart';

class ViderListTile extends StatelessWidget {
  const ViderListTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
