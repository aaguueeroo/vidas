import 'package:flutter/material.dart';

class RelationshipTypeButton extends StatelessWidget {
  final String text;
  final IconData icon;
  const RelationshipTypeButton({Key? key, required this.text, required this.icon}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Placeholder();
  }
}
